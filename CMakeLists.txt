cmake_minimum_required(VERSION 3.3)

project(crash-samples)

option(double-free "Include double-free." ON)
option(corrupted "Include corrupted." ON)
option(__cxa_pure_virtual "Include __cxa_pure_virtual." ON)
option(terminate "Include terminate." ON)
option(missing-return-statement "Include missing-return-statement." ON)
option(race-condition "Include race-condition." ON)
option(static-initialization-order "Include static-initialization-order." ON)
option(M_realloc_insert "Include M_realloc_insert." ON)

if(double-free)
    add_subdirectory(double-free)
endif()
if(corrupted)
    add_subdirectory(corrupted)
endif()
if(__cxa_pure_virtual)
    add_subdirectory(__cxa_pure_virtual)
endif()
if(terminate)
    add_subdirectory(terminate)
endif()
if(missing-return-statement)
    add_subdirectory(missing-return-statement)
endif()
if(race-condition)
    add_subdirectory(race-condition)
endif()
if(static-initialization-order)
    add_subdirectory(static-initialization-order)
endif()
if(M_realloc_insert)
    add_subdirectory(M_realloc_insert)
endif()
